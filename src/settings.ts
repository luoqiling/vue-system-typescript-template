interface ISettings {
  title: string
  name: string
}

const settings: ISettings = {
  title: 'vue系统模板',
  name: 'vuesystemtypescripttemplate' // 通常用于cookie和storage的命名，不要写入特殊字符
}

export default settings
